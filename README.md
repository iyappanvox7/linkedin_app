# LinkedIn Scraper Dashboard

This dashboard coordinates with the Flask web server to allow triggering scrapers and reading results.

## Execution
1. Install node dependencies: `npm install`
2. Start the dev server: `npm start`
3. Runs at `http://localhost:3000` by default.
